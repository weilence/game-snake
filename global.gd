extends Node

const MAX_APPLE_COUNT = 5
var CELL_SIZE: int = 32
@onready var size: Vector2 = get_viewport().get_visible_rect().size / CELL_SIZE

# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass # Replace with function body.
